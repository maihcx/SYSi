<Window x:Class="SYSi.BugTracker.ReportPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Report Bug"
        Height="140"
        Width="260"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Window.Resources>
        <Style x:Key="OptionBtn"
               TargetType="Button">
            <Setter Property="Background"
                    Value="#1E293B" />
            <Setter Property="Foreground"
                    Value="#CBD5E1" />
            <Setter Property="BorderBrush"
                    Value="#334155" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="FontSize"
                    Value="13" />
            <Setter Property="Height"
                    Value="40" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="12,0">
                            <ContentPresenter HorizontalAlignment="Left"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter Property="Background"
                                        Value="#334155" />
                                <Setter Property="BorderBrush"
                                        Value="#475569" />
                            </Trigger>
                            <Trigger Property="IsPressed"
                                     Value="True">
                                <Setter Property="Background"
                                        Value="#0F172A" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#0F1F35"
            BorderBrush="#1E3A5F"
            BorderThickness="1"
            CornerRadius="10"
            Padding="10">
        <Border.Effect>
            <DropShadowEffect Color="#000000"
                              BlurRadius="20"
                              ShadowDepth="4"
                              Opacity="0.6" />
        </Border.Effect>

        <StackPanel>
            <TextBlock Text="Report to..."
                       Foreground="#64748B"
                       FontSize="11"
                       Margin="4,2,0,8" />

            <Button Style="{StaticResource OptionBtn}"
                    Margin="0,0,0,6"
                    Click="Facebook_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="💬"
                               FontSize="15"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0" />
                    <TextBlock Text="Facebook Messenger"
                               VerticalAlignment="Center" />
                </StackPanel>
            </Button>

            <Button Style="{StaticResource OptionBtn}"
                    Click="GitHub_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🐙"
                               FontSize="15"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0" />
                    <TextBlock Text="GitHub Issues"
                               VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </StackPanel>
    </Border>
</Window>