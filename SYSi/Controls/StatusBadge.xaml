<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:SYSi.Controls"
                    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <Style TargetType="{x:Type controls:StatusBadge}">
        <Setter Property="Focusable"
                Value="False" />
        <Setter Property="SnapsToDevicePixels"
                Value="True" />
        <Setter Property="OverridesDefaultStyle"
                Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:StatusBadge}">
                    <Border x:Name="BadgeBorder"
                            CornerRadius="{DynamicResource ControlCornerRadius}"
                            Padding="11,5"
                            Background="{DynamicResource SystemFillColorCautionBackgroundBrush}">
                        <TextBlock x:Name="BadgeText"
                                   Text="{TemplateBinding Text}"
                                   FontSize="12"
                                   TextTrimming="CharacterEllipsis"
                                   Foreground="{DynamicResource SystemFillColorCautionBrush}" />
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSuccess"
                                 Value="True">
                            <Setter TargetName="BadgeBorder"
                                    Property="Background"
                                    Value="{DynamicResource SystemFillColorSuccessBackgroundBrush}" />
                            <Setter TargetName="BadgeText"
                                    Property="Foreground"
                                    Value="{DynamicResource SystemFillColorSuccessBrush}" />
                        </Trigger>

                        <Trigger Property="IsHighContrast"
                                 Value="True">
                            <Setter TargetName="BadgeBorder"
                                    Property="Background"
                                    Value="Transparent" />
                            <Setter TargetName="BadgeBorder"
                                    Property="BorderBrush"
                                    Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" />
                            <Setter TargetName="BadgeBorder"
                                    Property="BorderThickness"
                                    Value="1" />
                            <Setter TargetName="BadgeText"
                                    Property="Foreground"
                                    Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>